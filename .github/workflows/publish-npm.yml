name: Publish Node.js Package to GitHub Packages 🇺🇸 / Publicar Pacote Node.js 🇧🇷

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  publish-npm:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository 🇺🇸 / Baixar repositório 🇧🇷
        uses: actions/checkout@v4

      - name: Setup Node.js 🇺🇸 / Configurar Node.js 🇧🇷
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@TAVARDT'

      - name: Install dependencies 🇺🇸 / Instalar dependências 🇧🇷
        run: npm install

      - name: Publish to GitHub Packages 🇺🇸 / Publicar no GitHub Packages 🇧🇷
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
